package util;

import javax.swing.*;
import java.awt.event.ActionListener;
import java.awt.event.ActionEvent;
import java.awt.*;
import java.util.ArrayList;

/**
 * HeadersComboBox.java  Date Created: Apr 30, 2013
 *
 * Purpose: To extends JComboBox and add functionality to have headers.
 *
 * Description:
 *
 * @author Chrisb
 */
public class HeadersComboBox {/*extends JComboBox implements ListCellRenderer, ActionListener {

    public HeadersComboBox(HeaderComboItem[] items) {
        setRenderer(this);
        addActionListener(this);
    }

    public void addItem(HeaderComboItem item) {
        super.addItem(item.getItem());
    }

    public void actionPerformed(ActionEvent e) {

    }

    public Component getListCellRendererComponent(JList list, Object value, int index, boolean isSelected, boolean cellHasFocus) {
        if (isSelected) {
            setBackground(list.getSelectionBackground());
            setForeground(list.getSelectionForeground());
        } else {
            setBackground(list.getBackground());
            setForeground(list.getForeground());
        }
        if ( ((HeaderComboItem)value).isHeader() ) {
//            setBackground();
        }
    }*/

//    private Object selectedItem;
//    private int[] headerIndexes;
}